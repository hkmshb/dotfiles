alias p-p='poetry publish --build -r pypi-test -u ${PYPI_USER}'
alias p-pl='poetry publish --build -u ${PYPI_USER}'

alias pyenv-d='pyenv deactivate'
alias pyenv-a='pyenv activate $*'

alias pip='python -m pip'

alias poet='pipx run poetry'
alias poetry='pipx run poetry'
alias precom='pipx run pre-commit'
alias cookiecutter='pipx run cookiecutter'
