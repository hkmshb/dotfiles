#!/bin/bash

brew bundle -v --file=/dev/stdin <<EOF
{{ range .packages.brews -}}
brew {{. | quote}}
{{ end -}}

{{ range .packages.casks -}}
cask {{. | quote}}
{{ end -}}
EOF

# define an env var to indicate when to install large casks

# install mise packages
mise install
